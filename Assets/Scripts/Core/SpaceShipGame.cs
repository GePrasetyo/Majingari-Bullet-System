using Majinfwork;

namespace BulletSystem {
    public class SpaceShipGame : GameInstance {
        public GameStats Stats { get; private set; } = new GameStats();

        protected override void Start() {
            base.Start();
        }

        protected override void Tick() {
            base.Tick();
        }

        protected override void Stop() {

        }
    }
}